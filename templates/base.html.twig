<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon"
          href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {% block stylesheets %}
    {% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body>
<div class="container-fluid">
    <header class="pt-5">
        <p class="h1">Symfony Avancé - POE PHP</p>
        <hr/>
    </header>
    <nav class="nav">
        <a href="{{ path('home_index') }}" class="nav-link">Accueil</a>
        <a href="{{ path('service_index') }}" class="nav-link">Les Services</a>
        <a href="{{ path('service_contact_form') }}" class="nav-link">Contactez nous</a>
        <a href="{{ path('contact_index') }}" class="nav-link">Liste des contacts</a>
        <a href="{{ path('http_index') }}" class="nav-link">Liste des Utilisateurs API</a>
        <a href="{{ path('http_jsonplaceholder') }}" class="nav-link">Liste des Utilisateurs API avec config</a>
        <a href="{{ path('turbo_index') }}" class="nav-link">Turbo</a>
    </nav>
    <main>
        {% block body %}{% endblock %}
    </main>
    <footer>
        <hr/>
        <p class="small text-center">Dawan FOAD - POEI PHP - Décembre 2023</p>
    </footer>
    <div class="toast-container p-3 top-0 end-0" id="notification">
        {{ include('notification.html.twig') }}
    </div>
</div>
</body>
</html>
